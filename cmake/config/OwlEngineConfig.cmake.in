get_filename_component(PACKAGE_PREFIX_DIR "${CMAKE_CURRENT_LIST_DIR}/../" ABSOLUTE)

set(PN Reshaper)

if (NOT ${PN}_FOUND)
    set(${PN}_FOUND ON)
    include("${CMAKE_CURRENT_LIST_DIR}/${PN}Targets${CMAKE_SYSTEM_NAME}.cmake")


endif ()
